spring:
  application:
    name: backend

# D-ID integration settings (set via environment variables for security)
did:
  api:
    url: ${DID_API_URL:}
    key: ${DID_API_KEY:}
  avatar:
    id: ${DID_AVATAR_ID:}
